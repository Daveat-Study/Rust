## To Prevent warning UNUSE_VARIABLE

#![allow(unused)]

fn main(){
    println!("Hello, world");
}

# {:?}
The {:?} format specifier is used to print the value of a variable or expression in a human-readable format.